#include <stdio.h>

void TowerOfHanoi(int n, char src, char dest, char aux) {
  
    TowerOfHanoi(n ,int j ,int c ,int b) {
        if (n == 1) {
            printf("Move disk 1 from %c to %c",j ,c);
            return;
        }
        TowerOfHanoi(n-1,j,b,c);
        printf("Move disk %d from %c to %c",n,j,)
    }
}